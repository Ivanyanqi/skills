# 安装说明

- 这个 skill 目前只面向 macOS。
- 如果机器缺少 Xcode Command Line Tools，macOS 可能会弹出系统安装窗口。
- 如果目标机器上的 `iTerm2` 从未启动过，请先打开一次，再重新运行 profile 配置脚本。
- 这个 skill 有意排除了 `conda`、`nvm`、`bun` 这类个人运行时初始化内容。
- 源机器上的壁纸文件不会被打包。现在这套玻璃感主要来自透明度和模糊效果。
